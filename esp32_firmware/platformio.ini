[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 921600
monitor_filters = esp32_exception_decoder

; Ensure enough flash for the RF model header
board_build.partitions = default.csv

; Auto-detect COM port (or set manually)
; upload_port = COM4
